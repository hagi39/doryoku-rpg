# CLAUDE.md

このリポジトリで作業するときに、最初に読むファイル。
中身は README.md に書いてあるので、ここでは**どこを見ればよいか**だけを示す(二重に書かない)。

## 必ず守ること

**既存のファイルを作り直す・書き換える前に、必ず中身を先に確認する。**
とくに `cat > ファイル <<'EOF'` のように、ファイル全体を一括で上書きするコマンドは要注意。
「新しく作る」つもりのパスでも、先に確認してから触ること。

→ 理由・具体的な手順・実際にやらかした例は、[README.md](README.md) の **「作業のルール」** に書いてある。
作業を始める前に必ず読むこと。

## 機能を足すとき

**コードを変更する前に、何をどう変えるかを見せて確認をとる。**
機能は1つずつ(多くても2つずつ)公開し、セーブデータが更新後も使えることを
テストで確かめてから公開する。

→ 方針と、次に作る予定のものは、[README.md](README.md) の **「開発の進め方」**。

## 変更したら

```sh
npm run check   # ビルド + テスト + 成果物の構文/自己完結チェック
```

これが通らないものはコミットしない。`main` に push すると GitHub Pages
(https://hagi39.github.io/doryoku-rpg/) が自動で更新される。
テストか成果物チェックが落ちた場合は公開されない。

## 調べたいことの参照先

すべて [README.md](README.md) の各節。

| 知りたいこと | 節 |
|---|---|
| ビルド・テストの回し方 | 使い方 |
| 開発の進め方、次に作る予定のもの | 開発の進め方 |
| ファイル構成、ビルドへの追加手順 | ディレクトリ |
| アプリの方針(配布・AI・APIキー) | 決まっていること |
| `file://` で使うときの注意 | `file://` で使うときの注意 |
| スマホなどへの配り方、容量の実測値 | 受け渡し(P7で確認したこと) |
| どこまで作ったか | 実装フェーズ |
| 設計上の判断と、その理由 | 設計書で未確定だった点の判断 |

仕様そのものは `努力RPG_設計書_ClaudeCode実装用.md`。
README の「設計書で未確定だった点の判断」は、設計書で決まっていなかった部分を
どう決めたかの記録なので、実装前に目を通すと手戻りが減る。

## AIの出力に問題が見つかったとき

このアプリは AI に教材と問題を作らせる。問題文や教材の書き方がおかしいときは、
`sanitizeQuestions` での検査を増やすのではなく、`quiz-gen.js` の `FORMAT_RULES` と
`material-gen.js` の `TEXT_RULES` に指示を足して直す。

→ 方針とこれまでに足したルールは、README.md の「設計書で未確定だった点の判断」13番。

直したあとは、**実際に生成させて結果を読むまで「直った」と言わない。**
テストが通ることと、AI の書き方が直っていることは別。
